# Overlay production — utiliser avec :
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
#
# Variables lues depuis .env (voir .env.production.example).
# ports: !reset [] — obligatoire pour effacer les ports du docker-compose.yml de base
# (sinon Compose les concatène et provoque « address already in use »).
# Seul Caddy est exposé publiquement (ports configurables via CADDY_HTTP_PORT / CADDY_HTTPS_PORT).
# Let's Encrypt HTTP-01 exige que CADDY_HTTP_PORT soit 80 sauf challenge DNS.

services:
  postgres:
    ports: !reset []
    environment:
      POSTGRES_PASSWORD: ${DB_PASS:?DB_PASS requis dans .env}
      POSTGRES_HOST_AUTH_METHOD: scram-sha-256
    restart: unless-stopped

  pgbouncer:
    ports: !reset []
    environment:
      DATABASE_URL: postgres://${DB_USER:-firstpay}:${DB_PASS}@postgres:5432/firstpay
    restart: unless-stopped

  kafka:
    ports: !reset []
    restart: unless-stopped

  redis:
    ports: !reset []
    restart: unless-stopped

  api-gateway:
    ports: !reset []
    environment:
      SPRING_PROFILES_ACTIVE: prod
      FRONTEND_ORIGIN: ${FRONTEND_ORIGIN:-https://esign.afbdei.com}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET requis dans .env}
      INTERNAL_TOKEN: ${INTERNAL_TOKEN:-}
      TENANT_FALLBACK_ENABLED: "false"
      TRACING_SAMPLE_RATE: ${TRACING_SAMPLE_RATE:-0.05}
    restart: unless-stopped

  transaction-service:
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_USER: ${DB_USER:-firstpay}
      DB_PASS: ${DB_PASS}
      TRACING_SAMPLE_RATE: ${TRACING_SAMPLE_RATE:-0.05}
    restart: unless-stopped

  partner-service:
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_USER: ${DB_USER:-firstpay}
      DB_PASS: ${DB_PASS}
      JWT_SECRET: ${JWT_SECRET}
      INTERNAL_TOKEN: ${INTERNAL_TOKEN:-}
      # Filet de sécurité pour les liens email si le host de la requête est indisponible.
      APP_PUBLIC_BASE_URL: ${APP_PUBLIC_BASE_URL:-${FRONTEND_ORIGIN:-https://esign.afbdei.com}}
      TRACING_SAMPLE_RATE: ${TRACING_SAMPLE_RATE:-0.05}
    restart: unless-stopped

  payment-service:
    environment:
      SPRING_PROFILES_ACTIVE: prod
      PAYMENT_WEBHOOK_BASE: ${PAYMENT_WEBHOOK_BASE:-https://esign.afbdei.com/webhooks/trustpayway}
      PAYMENT_SIMULATION_ENABLED: ${PAYMENT_SIMULATION_ENABLED:-false}
      TRACING_SAMPLE_RATE: ${TRACING_SAMPLE_RATE:-0.05}
    restart: unless-stopped

  reporting-service:
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_USER: ${DB_USER:-firstpay}
      DB_PASS: ${DB_PASS}
      # VPS mono-nœud : pas de réplica — lectures sur le primaire
      READ_DB_URL: r2dbc:postgresql://postgres:5432/firstpay
      TRACING_SAMPLE_RATE: ${TRACING_SAMPLE_RATE:-0.05}
    depends_on:
      pgbouncer:
        condition: service_started
      kafka:
        condition: service_started
      transaction-service:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    ports: !reset []
    restart: unless-stopped

  payer-frontend:
    ports: !reset []
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "${CADDY_HTTP_PORT:-80}:80"
      - "${CADDY_HTTPS_PORT:-443}:443"
    volumes:
      - ./infrastructure/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend
      - api-gateway
      - payment-service
    networks:
      - default

volumes:
  caddy_data:
  caddy_config:
